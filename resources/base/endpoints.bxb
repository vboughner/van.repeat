endpoints {
  action-endpoints {
    action-endpoint (RepeatAction) {
      accepted-inputs (repeatInput, voiceName)
      local-endpoint ("RepeatAction.js")
    }
    action-endpoint (GetVoiceStyles) {
      local-endpoint (GetVoiceStyles.js)
    }
  }
}
